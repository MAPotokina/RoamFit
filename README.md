# ROAMFIT

Multi-agentic fitness application designed to help users maintain their fitness routine while traveling.

## Setup

1. Install dependencies: `pip install -r requirements.txt`
2. Copy `.env.example` to `.env` and add your OpenAI API key
3. Run: `streamlit run main.py` (once main.py is created)

## Project Structure

See [vision.md](./vision.md) for technical architecture and [doc/tasklist.md](./doc/tasklist.md) for development plan.

